/**
 * @class exit_game
 * @brief Exception indiquant qu'il faut arrêter la partie
 */
class exit_game: public std::exception {
		/**
		 * @brief Accesseur de message
		 * @return Message d'erreur
		 */
		virtual const char* what() const throw() {
			return "Player wants to exit the game";
		}
};